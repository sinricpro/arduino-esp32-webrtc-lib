// List Arduino dependencies here so PlatformIO's scanner can discover them.
#include <WiFi.h>
#include <WebServer.h>
#include <ESP_I2S.h>

// Reuse the Arduino example and its editable Settings.h / CameraConfig.h.
#include "../../Doorbell/Doorbell.ino"
